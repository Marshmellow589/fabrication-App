- Username: testuser
- Password: testpass

#Termintate all the uvicorn port and process
taskkill /IM uvicorn.exe /F
#uvicorn start
uvicorn fabrication_app.app.main:app --host 0.0.0.0 --port 8000 --reload
#chect port status 
netstat -ano | findstr :8000

#terminate process with desgined process no 108692 
taskkill /PID 108692 /F

#restart fabricatino app with port no 8001
uvicorn fabrication_app.app.main:app --host 0.0.0.0 --port 80012 --reload
#start up local host with port no:8001
visit http://localhost:8001
#pwer shell command to identify and kill process on port 8001
Get-NetTCPConnection -LocalPort 8001 | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }